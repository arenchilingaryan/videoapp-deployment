apiVersion: v1
kind: Service
metadata:
  name: service-1
spec:
  selector:
    app: my-app-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: service-2
spec:
  selector:
    app: my-app-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080